import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#074F57',
        },
        secondary: {
            main: '#FFFFFF',
        },
        background: {
            default: '#FFFFFF',
            drawer: '#074F57',
        },
    },
});

export default theme;